spring:
  application.name: commodity-service

---
eureka.client.serviceUrl.defaultZone: http://localhost:8761/eureka/

---
spring.cloud.config:
  discovery:
    enabled: true
    serviceId: config-server

spring.sleuth.sampler.percentage: 1